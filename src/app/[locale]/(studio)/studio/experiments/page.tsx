// The A/B Studio reuses the full experiments UI (native + GrowthBook + Convert
// + EYE revenue overlay) inside the Studio chrome. Single source of truth.
export { default } from "@/app/[locale]/(app)/dashboard/experiments/page";
